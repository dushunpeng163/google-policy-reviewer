# ============================================================
# 游戏开发工作坊 - AI 配置
# ============================================================
# 使用方法：
#   cp .env.example .env
#   在 .env 中填写你的 API Key，然后重启服务器
#
# 注意：.env 文件不会提交到 Git（已加入 .gitignore）
# ============================================================

# LLM 提供商：anthropic（默认）或 openai
LLM_PROVIDER=anthropic

# Anthropic Claude API Key
# 获取地址：https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-你的Key填在这里

# OpenAI API Key（如果使用 OpenAI 则填写此项）
# 获取地址：https://platform.openai.com
# OPENAI_API_KEY=sk-你的Key填在这里

# 模型选择（可选，注释掉则使用默认值）
# 默认：anthropic 使用 claude-sonnet-4-5，openai 使用 gpt-4o
# LLM_MODEL=claude-sonnet-4-5

# 最大输出 Token 数（默认 4096）
# LLM_MAX_TOKENS=4096
